name = "Google Mail"
description = "Keeps your Gmail on your server."
owner = "system"
command = ["google_gmail_sync"]
triggers = ["cron", "manual"]
schedule = "0 */15 * * * *"
per_credential = true
source = { id = "google" }

# Ontologies this applet writes — drives "what would fill this stream".
writes = ["communication_email"]
