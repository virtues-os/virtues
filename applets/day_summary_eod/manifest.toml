name = "Day Summary (End of Day)"
description = "Writes the day's entry in your autobiography once the day is done."
owner = "system"
runtime = "function"
command = ["day_summary_eod"]
triggers = ["cron", "manual", "tool"]
default_cron = "0 0 * * * *"
