name = "Notion Pages"
description = "Keeps your Notion pages and databases here."
owner = "system"
command = ["notion_pages_sync"]
triggers = ["cron", "manual"]
schedule = "0 */30 * * * *"
per_credential = true
source = { id = "notion" }

# Ontologies this applet writes — drives "what would fill this stream".
writes = ["content_document"]
