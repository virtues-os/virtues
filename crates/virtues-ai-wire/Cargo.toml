[package]
name = "virtues-ai-wire"
version = "0.1.0"
edition = "2021"
authors = ["Virtues Team <team@virtues.com>"]
description = "The one request type a box sends to /v1/ai/chat/completions and the proxy reads. A field exists on both sides or does not compile."
license = "MIT"
repository = "https://github.com/virtues-os/virtues"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
