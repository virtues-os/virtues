//! OAuth token refresh helpers.
//!
//! Stub — populated when OAuth sources (Google, Strava, Notion, etc.) migrate
//! to the actions architecture. Will host per-provider refresh logic and a
//! shared `refresh_if_expired(db, credential_id)` helper.
