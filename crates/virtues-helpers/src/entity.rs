//! Cross-source entity resolution helpers.
//!
//! Stub — populated when we migrate entity resolution (place clustering,
//! people matching) into the actions architecture. For now, contacts
//! resolution lives inside `ios_contacts` as a self-contained function.
