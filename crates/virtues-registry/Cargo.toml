[package]
name = "virtues-registry"
version = "0.1.0"
edition = "2021"
authors = ["Virtues Team <team@virtues.com>"]
description = "Shared registry of models, agents, tools, sources, streams, and ontologies"
license = "MIT"
repository = "https://github.com/virtues-os/virtues"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
