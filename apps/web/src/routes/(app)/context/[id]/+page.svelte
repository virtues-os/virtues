<script lang="ts">
	// This route exists to prevent 404 errors when navigating directly to /context/[id]
	// The actual rendering is handled by the tab system (ContextView component)
	// via TabContent.svelte based on the 'session-context' tab type.
</script>
