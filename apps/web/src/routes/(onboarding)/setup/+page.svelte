<!--
  Never rendered — `+page.ts` redirects to /onboarding before this loads.
  SvelteKit requires a `+page.svelte` for the route to exist at all, and the
  route has to exist for the redirect to be reachable.
-->
<p>Redirecting…</p>
