[package]
name = "virtues-plugin-lockstep"
version = "0.1.0"
authors = ["Virtues"]
description = "Build-time lockstep for the local Tauri plugins: build.rs COMMANDS is the single source of truth, everything else is verified or generated from it."
edition = "2021"

[dependencies]

# Standalone (excluded from the repo workspace). The empty table stops cargo's
# workspace walk-up, which otherwise escapes the repo in nested git worktrees
# and gets claimed by an outer checkout's manifest.
[workspace]
