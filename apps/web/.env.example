# Application Database (SvelteKit/Drizzle)
# Uses the 'app' schema in the ariata database
# Stores UI preferences, dashboards, saved queries
DATABASE_URL=postgresql://localhost:5432/ariata

# Rust ELT Backend API URL
# The Vite proxy forwards /api/* requests here
ELT_API_URL=http://localhost:8000
