name = "Mac Ingest"
description = "Ingest app usage, browser activity, and iMessage history from a paired Mac."
owner = "system"
runtime = "function"
command = ["mac_ingest"]
triggers = ["webhook"]
per_credential = true
source = { id = "mac" }
