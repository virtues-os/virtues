name = "Plaid Investments"
description = "Sync brokerage holdings and securities for a Plaid Hosted Link credential."
owner = "system"
runtime = "function"
command = ["plaid_investments_sync"]
triggers = ["cron", "manual"]
default_cron = "0 0 */12 * * *"
per_credential = true
source = { id = "plaid" }
