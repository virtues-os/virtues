name = "iOS Microphone"
description = "Ingest audio chunks from a paired iPhone for transcription."
owner = "system"
runtime = "function"
command = ["ios_microphone"]
triggers = ["webhook"]
per_credential = true
source = { id = "ios" }
