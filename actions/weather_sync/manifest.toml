name = "Weather Sync"
description = "Hourly current-conditions + short forecast from Open-Meteo for the owner's (rounded) location. Establishes the environment ontology; no key, no tracking."
owner = "system"
runtime = "function"
command = ["weather_sync"]
triggers = ["cron", "manual"]
default_cron = "0 0 * * * *"
