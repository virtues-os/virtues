name = "Plaid Liabilities"
description = "Sync credit cards, mortgages, and student loans for a Plaid Hosted Link credential."
owner = "system"
runtime = "function"
command = ["plaid_liabilities_sync"]
triggers = ["cron", "manual"]
default_cron = "0 0 */12 * * *"
per_credential = true
source = { id = "plaid" }
