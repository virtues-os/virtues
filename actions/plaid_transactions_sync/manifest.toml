name = "Plaid Transactions"
description = "Sync transactions for a Plaid Hosted Link credential. Uses incremental cursor."
owner = "system"
runtime = "function"
command = ["plaid_transactions_sync"]
triggers = ["cron", "manual"]
default_cron = "0 */30 * * * *"
per_credential = true
source = { id = "plaid" }
