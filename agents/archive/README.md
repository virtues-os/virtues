# Archive — superseded

Kept for the reasoning, not the content. Nothing here describes the system as
it is, and nothing here publishes. If you are citing one of these, cite why it
was replaced rather than what it says.


| Doc | Status | What it's for |
|---|---|---|
| [bench/](bench/) | Tools | The four measurement scripts the spec below was written for, kept runnable and stdlib-only. `bench.py` is that spec's Tests A and B — it starts nothing, you point it at whatever sidecars are listening and label the run. `gpu_embed_bench.sh` is the confirm-or-refute run behind the shipped embed→CPU / rerank→GPU split (EmbeddingGemma's activations force fp32 on CUDA — a model property, not a llama.cpp bug). `embed_doc_bench.py` times one real document becoming searchable; `encyclical_test.py` is the only one still on the current stack, running the full production-shaped chunk → embed → retrieve → rerank pipeline against the standing test corpus. |
| [getting-started-page-plan.md](getting-started-page-plan.md) | Superseded | Getting started as Home's first-run state: a numbered seven-step page that sheds sections as they retire (built 2026-08-31, `2e4d99e7`). Superseded 2026-09-08 by [../record/getting-started.md](../record/getting-started.md), which moves getting started into one chat room. Its reasoning about pipeline timing (why a one-pass reveal cannot work) still holds. |
| [inference-bench-spec.md](inference-bench-spec.md) | Answered | The board bench that chose between Jetson and Qualcomm Q6A, narrowed to the only two moments a user can feel — live rerank mid-chat, and a 100-page PDF becoming searchable — with a rubric honest enough to conclude the boards are a wash and decide on cost and heat instead. **Answered**: the results are [npu-hardware-findings.md](../record/npu-hardware-findings.md) and the Q6A is the board. Its system under test is also two model generations stale (bge-m3 / bge-reranker-v2-m3, where the shipped stack is EmbeddingGemma-300M + gte-reranker-modernbert), so what transfers is the framing of *what* to measure, not the flags. Tools in [bench/](bench/). |
| [ios-overhaul-plan.md](ios-overhaul-plan.md) | History | Full audit of the **native Swift iOS app**, which has since been deleted — the live iOS app is the Tauri build of `apps/web`. The delivery, durability, and narrative findings still transfer; anything about Swift views does not. |
| [networking-relay-tee.md](networking-relay-tee.md) | Superseded | The pre-iroh ADR: single-hop SNI relay, per-box ACME, browser-anywhere. Its privacy hardening — RAM-only, blinded tokens, unlinkability, audit — was **proposed and never built**. History only; do not quote. |
| [relay-control-plane.md](relay-control-plane.md) | Design notes | Naming, auth, and revocation reasoning. Its SNI/HMAC mechanism was never built; the *iroh caveat* section is the honest statement of what the relay observes. |
| [stories-plan.md](stories-plan.md) | Cut | The full design exploration behind entity and temporal resolution, significance, and the `wiki_stories` unification — the "stay as dust or resolve" branch with dust as the default and majority case, narrative as a lossy re-derivable *sample* of evidence and never the reverse, and maintenance bounded by what the person chose to claim. **Cut from v1 on 2026-07-22**: a spike on real data could not establish who the claim-style story helps. `wiki_stories` is dropped, notebooks are the surviving primitive, and the six-witness significance quorum went earlier on its own merits. Kept for the grounding chapter and the vocabulary; the spike's durable findings live with the magnet code, not here. |
| [things.md](things.md) | Removed | `wiki_things` as it briefly existed — a deliberately light folder you could re-enter, carrying a name, URL pins, and a "where you left off" memo. **Removed 2026-07-21**: the table is dropped, `/api/things` and the `api::things` module are gone. stories-plan.md replaced it — projects and hobbies become stories, concepts become topics, and a rare mattering particular accumulates as a floating mention instead of earning its own entity. |
| [relay-walkthrough.html](relay-walkthrough.html) | Superseded | Visual lifecycle diagrams of the old SNI relay. Kept as a record. |
