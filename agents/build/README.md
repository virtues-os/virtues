# Build — how it must be done

**Normative and permanent.** These say what must be true when you write code
here: contracts, vocabularies, style, and the runbooks for operations we
perform ourselves. They are maintained rather than dated — when the system
changes, these change with it.

Read the ones touching your area BEFORE you write. They exist because the same
mistakes kept recurring across sessions.

**These never publish.** They are instructions to whoever is building, only
meaningful with the codebase open. `virtues.com/docs` is the manual, written
for people running a box; see [`docs/`](../../docs/).


| Doc | Status | What it's for |
|---|---|---|
| [appliance-image.md](appliance-image.md) | Partly built | How a Dragon becomes a shippable unit: the card-boots/NVMe-roots truth read off real hardware, the OS-on-card/data-on-NVMe split and why, `tools/build-dragon.sh`, the deprovision → `image-check` → `dd` gate, and the three questions that need the bench. |
| [architecture.md](architecture.md) | Current | The implementation contract for the **applet** system: `function` / `view` runtimes, manifest↔SQL field ownership, dispatch + reconcile, the two applet roots. Authoring how-to is [`applets/AUTHORING.md`](../../applets/AUTHORING.md). |
| [codemirror.md](codemirror.md) | Current | The page editor's document model. The document **is** markdown in a Yjs `Y.Text`; decorations provide live preview. No intermediate AST. |
| [composable-inference.md](composable-inference.md) | Current | Plan of record for inference composability: the two HTTP contracts (`/v1/embeddings` required, `/v1/rerank` optional) and how the index defends itself. |
| [deployment.md](deployment.md) | Current | The two shipping shapes — native Linux binary on the box, Docker on EC2 for atlas + api — and the systemd privilege split. |
| [design-grammar.md](design-grammar.md) | Current | The **page** grammar (design.md is the shell): one sentence per page; **turns, not blocks** — the speaks · shows · opens · asks · answers grammar of authorship, where *a rule is the box talking and a card is you answering*, and framed-or-unframed is the only distinction the code carries; the four page genres and the one measure a room is allowed; the type roles and scale; one accent with two meanings; three from-only animations; and a countable checklist that `tools/design-lint.sh` ratchets against a frozen baseline. Written 2026-09-04 after Getting Started was redesigned **eight** times with no rule to settle it, and rewritten against the tree 2026-09-16. The spread and its frontispiece are not the grammar any more — they sit in **Struck**, which keeps what killed them because that outlives the thing. |
| [design.md](design.md) | Current | Shell design constraints. Exists because the same visual mistakes kept recurring across sessions. |
| [entitlement.md](entitlement.md) | Current | How a paying box gets AI and utility calls paid for: accounts, a rotatable device api_key, an append-only ledger, Stripe-webhook crediting. **§1 states the privacy posture plainly — read it before making any privacy claim.** |
| [glossary.md](glossary.md) | Current | One vocabulary for a life in time, and the law it settles: the three axes, which temporal primitives earn a page (day, year, chapter, story, life — week and month refused), what each tense owes, the four provenances, and the four kinds of gap. **"Gap" unqualified is banned, and "entity" is never a synonym for "subject"** — `wiki_refs.entity_type` and `wiki_articles.subject_type` differ on purpose, and the obvious tidy-up would be wrong. [`api/subjects.rs`](../../virtues-core/src/api/subjects.rs) is this page in code. |
| [mac-plan.md](mac-plan.md) | Partly built | Onboarding, permissions, and every update path the Mac has, written after a six-day silent outage (2026-07-30 → 08-05) that all three surfaces failed to report and in places misreported. The keystone is two identities: a release version owned by the box (what the user sees) and an opaque build counter (what the updater compares). **Phases 0–2 have since landed** — the counter is past the old 1.0.20 line, permission truth reaches the screen three-state with an observation time, and the per-device channel file is gone. Still open: the command-surface version of §"Declare the OTA contract". §7's open question went the other way — the Mac still shells to the box's live SPA. |
| [model-migration.md](model-migration.md) | Current | Runbook for changing the local embedding/rerank GGUFs. **Three places must agree** or the box serves one model while the runtime expects another. |
| [narrative-identity.md](narrative-identity.md) | Current | The canonical definition — the part of a person's context no record can derive, authored in their own words and read before every conversation. Carries the two iron rules (user-authored never inferred; the machine writes it only while it is empty), the ratification-not-composition line, why rules are a *neighboring* channel and not part of it, and the eight ordered system-prompt blocks with their precedence. **There is deliberately no abridged "core" beside the document** — that column existed, the two versions drifted, and the abridger was caught inventing standing directives. |
| [onboarding.md](onboarding.md) | Current | "Box in hand" → "Virtues earning its keep," as built — rewritten against the code 2026-08-28. **The one Bluetooth wire (the RPC table), the phrase vs the pair code, the display's seven ranked states, the two journeys (flashed appliance and DIY headless), and the walk from the founder's letter into Home.** It previously stacked three generations of doctrine behind a corrections preamble; the corrections are folded in and the superseded generations are kept only as failure history. Intent is [onboarding-paradigm.md](../record/onboarding-paradigm.md), which lives in `agents/record/`. |
| [recovery.md](recovery.md) | Current | **Workshop** operator runbook, rewritten against the code 2026-08-28 after an audit found ~19 stale claims (a config path no box has, three commands that don't exist, a dropped atlas column). Carries the operator-only material: the recovery command surface, the sudo gate, the diagnostic surface, and the described-but-not-implemented list. Owner-facing recovery is [`docs/operate/recovery.md`](../../docs/operate/recovery.md), which wins on any disagreement. |
| [review-access-plan.md](review-access-plan.md) | Current | How an Apple reviewer who owns no box exercises the iOS app. Live coordinates and the pair code are deliberately **not** in this repo. |
| [typography.md](typography.md) | Current | The type shelf as it is on disk: four families, every file a self-hosted `woff2` (a box runs on a LAN with no guaranteed egress, and the kiosk is Linux, where "it's a system font" is false for all of them), and which token names each. **JJannon ships exactly one cut, so the serif can never be bold and never italic** — ask for 700 and font matching stays *inside* the family and hands back the one ink with no warning; ask for italic and the browser synthesizes a slant. That is the file, not taste, and it is why there are two serif tokens over one face: `--font-serif-ui` is the same file with its vertical metrics corrected, for serif set beside something that is not text. The serif fallback is a system stack on purpose after Lora was deleted 2026-09-16 — a downloadable fallback is a second file fetched to cover a moment that ends when the first one lands. **Open, and only a human can close it: nothing here records whether the Avenir license covers self-hosting**, so don't add weights, don't copy the files into another repo, and don't remove them either. |
| [wiki-editor.md](wiki-editor.md) | Current | How the wiki's prose is instructed: one **constitution** (the rules that never vary) plus one **brief** per subject kind, kept as `.md` beside the code and compiled in, so the text reviewed is the text that runs. Records what is settled — second person, the three deliberate voices, editing rather than rewriting. |
| [virtues-api.md](virtues-api.md) | — | The philosophy and the FAQ-ready copy for the paid API — what may and may not be claimed. Lift wording from here; it is maintained, not dated. |
| [voice.md](voice.md) | Current | **There is no house voice** — the file says why, then holds the three things that survived: the **claim rules** every surface obeys ("cannot" not "will not"; artifacts not features; their word *data* against our word *the record*; show the architecture, don't assert the virtue), the **register for in-app copy** (clear before short before characterful; **a passive sentence is a missing subject — the person is the actor where the thing is theirs, the server is named where the server failed**; an error is what happened plus how to fix it; say the consequence before anything irreversible; split past 25 words; hyphens not em dashes; "computer" not "Mac"; one name per thing; verify a claim against the shipping build or delete it), and the **bank** of lines with the surface each is for, plus the name's reframe. `.claude/rules/copy.md` loads the copy section wherever screen prose lives. |
