# Plan — what we intend

**Normative and temporary.** Designs for things being built. A plan is the one
genre here with an expiry date: **when the thing ships, delete the plan.** What
survives is a record of what was built and a page in the manual — not a
document describing an intention that has since become a fact.

That rule is why this directory exists. `docs/` accreted 63 files because
nothing ever left it; a plan with no death condition becomes indistinguishable
from a description of the system, and gets read as one.

**These never publish.** A plan read by an outsider is a promise, and most of
these are not promises.


| Doc | Status | What it's for |
|---|---|---|
| [ai-door-plan.md](ai-door-plan.md) | Mostly built | One AI door: tokens are resolved, never guessed. The proxy stops inventing a 4096 cap and forwards the `reasoning` object; background jobs declare a thinking mode instead of a number; the stream emits `finish` with a reason. **Phases 1–4 are built on `wave`** — what is left is deploying the proxy and Phase 5. Replaces the two 16k hotfixes. |
| [applet-authoring-plan.md](applet-authoring-plan.md) | Planned | Phase 3: chat intent → folder → check → reconcile → gate → enabled applet. The capability and param-schema contract. |
| [applets-overhaul-plan.md](applets-overhaul-plan.md) | Planned | "A user-space systemd with an AI author." Design locked 2026-07-19. Supersedes architecture.md at the concept/UX layer; the execution engine stays. |
| [attention-plan.md](attention-plan.md) | Partly built | How the wiki learns which parts of a day mattered. A record has no tense — only calendar knows what time it is *about* — so aftermath is invisible by construction. Attention's four phases, what may be linked vs noted vs asserted, and the refuted list. |
| [audio-collector-plan.md](audio-collector-plan.md) | Partly built | Porting continuous mic capture to Tauri. The hardest collector: iOS audio-session lifecycle, and the only continuous, high-volume, binary stream. |
| [audio-schedule-places-plan.md](audio-schedule-places-plan.md) | Built, unverified on a device | When and where the mic listens: quiet hours generalize to a per-weekday schedule, and a short list of places on the phone mutes inside them. Both are gates on the tap (mute, never release); a muted marker lets the box tell "kept nothing, by choice" from a dead collector. **Built 2026-09-14; the device verification is the remaining gate.** |
| [backup-plan.md](backup-plan.md) | Shipped through Phase C — convert | Surviving the loss of the box. **Shipped through Phase C/13, verified against code 2026-08-28** — scheduler, age encryption, restore tests and the Settings panel included. The row here used to repeat the header the doc itself says it replaced ("both ends of the pipeline are still missing"); both ends shipped. What remains is a short gap list, which is all that still belongs in a plan. |
| [bookmarks-plan.md](bookmarks-plan.md) | v1 shipped — convert | Capture, enrichment, and retrieval for `data_content_bookmark`. **v1 shipped 2026-08-04/07**: three capture doors (in-app save, Mac browser bookmarks, GitHub stars), a budgeted enrichment sweep, and the room. The row here used to call it Planned with "zero producers"; there are four. |
| [byo-ai-plan.md](byo-ai-plan.md) | Mostly shipped | Splitting a slot's two axes — which model fills it, and whose credential pays for it — so BYO stops being an all-or-nothing switch that also overrides model choice. **About 60% shipped as of its own 2026-08-28 header, which lists the sentences below it that are now false** (including the body's "nothing here is built yet"). What remains is the thesis: the per-slot *route*, and with it many-credentials, slot probes, and the Image option list. Carries the limitation worth putting in the manual — BYO audio and transcription cannot work until the route exists, because the encoder has to know where the call is going. |
| [chat-ux-roadmap.md](chat-ux-roadmap.md) | Planned | The chat surface overhaul, in independently shippable tracks A–L, each ending in a verification gate. |
| [cross-platform-apps-plan.md](cross-platform-apps-plan.md) | Planned | Shipping the viewer on Windows, Linux, and Android. Collectors deliberately out of scope, with the seams left in place for them. |
| [display-plan.md](display-plan.md) | Partly built | Settings › Display: the box's face is a URL, with a precedence chain, an applet-face shelf, Hours, and a sleep engine the server owns rather than the panel. **Phases 1 and 2 shipped, Phase 3 about half** — what remains is the four built-in faces and the "ask for a new one" chat-authoring door. Its own header enumerates what the body below now gets wrong (the endpoint paths, the dropped EDID parse, three applets that no longer exist); the bench hardware findings moved to [the display-hardware record](../record/display-hardware.md). |
| [linking-plan.md](linking-plan.md) | Partly built | Step 2 of 3 — the account link, in depth: why linking must precede pairing (reach rides the relay and the relay rides the account, discovered live at an office), the inline sign-in contract, and the persona branch. **Its header diagram still says pairing uses "the on-screen code";** `0x83` has been codeless since 2026-08-24. |
| [mac-presence-plan.md](mac-presence-plan.md) | Planned | `data_activity_app_usage` is close to an inversion of the truth — real focused work is invisible while artifacts are the headline numbers. |
| [mobile-ux-plan.md](mobile-ux-plan.md) | Partly built | Pragmatic mobile next steps: keep the tab strip on phone, block split view, keyboard work gets a spike first. Edge-to-edge safe-area theming already shipped. |
| [narrative-resolution-plan.md](narrative-resolution-plan.md) | Planned | A standing queue of what the box does not know, ordered by what knowing is worth, asked one at a time where the answer pays off. Identity questions (a link, a merge) and meaning questions (prose, a rule) share a lifecycle and nothing else. |
| [notebooks-plan.md](notebooks-plan.md) | Partly built | Notebook as a workspace lens over the life-graph: a Library of materials, scoped grounded chat, source-level citations. |
| [onboarding-plan.md](onboarding-plan.md) | Partly built | The build order from what existed to the paradigm — the paradigm says what and why, this says in what order. **Its "Where we are" section is stale:** `0x82`/`0x83` are listed as "built, never observed" and were verified end-to-end on 2026-08-24 (see one-wire-plan.md), and the "three-step display" is now seven states. |
| [open-relay-plan.md](open-relay-plan.md) | Planned | The relay drops admission for a flat cap — reachability stops being a tier. The live relay's `/relay/authorize` callout is the privacy linkage to delete (the SNI/HMAC control plane turned out to be legacy prose); checkout leaves the airlock, pairing collapses to one token doctrine, clients go iroh-only. |
| [plaid-plan.md](plaid-plan.md) | Partly built | Plaid as the *general* finance collector — FinanceKit reaches only Apple's own products, so Plaid is the path that has to work for everyone else. |
| [publishing-plan.md](publishing-plan.md) | Planned | One frozen artifact, three surfaces (screen, print, publish). Publications replace page shares; a face is already the right container because its constraints ARE print's and static publishing's. v1 builds no ingress — the box keeps its zero internet-facing surface. |
| [reach-reliability-plan.md](reach-reliability-plan.md) | Mechanism shipped — convert | "100% reachable whenever the box is up." Root cause is upstream iroh #4289. **The mechanism is shipped** — `network_change()` passthrough, probe-then-rebuild keeping the EndpointId, and the Swift triggers — and the doc says so in its own header; only the polish remains. `apps/web/plugins/reach/src/lib.rs` cites this file by path, so sweep that when it becomes a record. |
| [references.md](references.md) | Planned | One primitive for `@`, peek, and open, so a person / place / file / page stops being rendered by a different code path each time. |
| [researcher-plan.md](researcher-plan.md) | Planned | The researcher/PhD archetype, built complete in v1 — corpus, annotation, scholarly metadata, synthesis bridge. Extends notebooks-plan Phase D. |
| [sources-packages-plan.md](sources-packages-plan.md) | Partly built | A source stops being a row in one compiled-in `applets/sources.toml` and becomes a *package* — a directory carrying its own `[[source]]` plus the applets that serve it — so adding a provider no longer costs a box release and a virtues-api deploy. **P0–P4 shipped 2026-08-04; P5 remains.** The decision is that a package *is* a git repo and installing a stranger's is first-class, sudo-gated, on the bet that making the git path load-bearing is what finally finishes the half-built importer. Its "What the sweep found" section describes the state *before* the work and is kept as rationale. |
| [specimens/establishing-screen/](specimens/establishing-screen/) | Specimen | A runnable draft of the beat the airlock lacks: one line, one control, and light, before setup asks its first question. Also the Bluetooth screen that does not exist today. Plates and score are gitignored — both are licensed for a production, and this repo is public. |
| [spa-delivery-plan.md](spa-delivery-plan.md) | Half built | One UI-delivery architecture for phone and Mac: baked bundle, OTA overlay pulled from the box, local data. The OTA half shipped (mobile); the Mac still renders the box's live SPA, and the offline pass is still blocked on the one-origin problem. UI-ahead-of-box skew turned out to be possible after all — the forward-only rule reintroduced it deliberately. |
| [ui-overhaul-plan.md](ui-overhaul-plan.md) | Planned | Nineteen layout/settings/design items triaged against the codebase, each marked ship / spike / defer / drop. |
| [version-compat-plan.md](version-compat-plan.md) | Planned | When the app outruns the server. Phones update themselves, servers update when someone types a command, so the app is always ahead — and v0.1.6 making `model` optional killed chat on every older server. Both version coordinates and the prerelease ordering already exist; what is missing is a declared minimum, a comparison, and a CI ratchet. |
| [update-manifold-plan.md](update-manifold-plan.md) | Planned | Generalizes the box paradigm to the whole fleet, and adds the cross-component version negotiation the box doc never needed. |
| [wiki-plan.md](wiki-plan.md) | Partly built | The personal wikipedia: every subject has one page, prose written from the record with citations, and an article is a page so the CRDT, history and marginalia come for free. Built to the day and the entity article. **Its §10 one-pen rule is overruled** by [agents/record/article-resolution.md](../record/article-resolution.md). |
